name: Update profile README

on:
  workflow_dispatch:
  schedule:
    # Refresh twice a day. GitHub schedules use UTC.
    - cron: "0 */12 * * *"

permissions:
  contents: write

env:
  # Opt in early to Node 24 for JavaScript actions while GitHub migrates runners.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout profile repo
        uses: actions/checkout@v4

      - name: Update merged PR section
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_USERNAME: praneethhere
          MAX_PRS: "8"
          BOT_MERGE_REPOS: pytorch/pytorch
        run: python .github/scripts/update_merged_prs.py

      - name: Commit README update
        run: |
          if git diff --quiet README.md; then
            echo "No README changes to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md
          git commit -m "Update merged open source PRs"
          git push
